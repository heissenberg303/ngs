console.dir(global, { depth: 0 });

// insert gloval object


// when use setTimeout -> actually is global.setTimeout

// is like Class is top of hierarchy in JAVA

// do not make variable attach to global variable